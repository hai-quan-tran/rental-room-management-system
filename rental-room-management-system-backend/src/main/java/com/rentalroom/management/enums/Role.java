package com.rentalroom.management.enums;

/**
 * Must stay in sync with the {@code chk_account_role} CHECK constraint in V1__init_schema.sql.
 */
public enum Role {
    ADMIN_TONG,
    ADMIN_CAP_1,
    USER
}
