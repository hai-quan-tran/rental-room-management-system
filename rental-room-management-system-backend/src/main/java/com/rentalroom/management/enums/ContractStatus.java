package com.rentalroom.management.enums;

/**
 * Must stay in sync with the {@code chk_contract_status} CHECK constraint in V1__init_schema.sql.
 */
public enum ContractStatus {
    ACTIVE,
    ENDED
}
